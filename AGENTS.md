# DataGround agent guidance

DataGround is security-sensitive platform infrastructure for data workloads, notebooks, coding agents, and persistent assistants. Correctness, fail-closed behavior, stable contracts, maintainability, and operational clarity take priority over expedient changes.

## Current repository state

This repository contains an executable foundation: control-plane process entry points are under `cmd`, internal Go packages are under `internal`, the React workbench is under `apps/workbench`, shared design tokens and accessible primitives are under `packages/tokens` and `packages/ui`, and canonical public contracts, examples, compatibility baselines, and generated types are under `contracts` and `apps/workbench/src/contracts`. The normative sources are `docs/architecture/decision-register.md` and `docs/architecture/system-specification.md`, in that order when they conflict. Implementation prerequisites, proposed choices, verification gates, and handoff prompts live under `docs/implementation/` and remain subordinate to those sources.

Do not invent repository paths, commands, supported behavior, dependency choices, or production guarantees. Introduce them only with the implementation and evidence that make them true, and update this guidance in the same change when they become durable repository facts.

The deterministic reference runtime has a loopback-only process-local mode and a PostgreSQL-backed durable development mode. Durable mode persists commands, explicit publication and invocation operations, event replay, idempotency, outbox and audit records, and fenced external-effect receipts through replaceable workers. The development-only OpenShell provider can persist isolation-scoped gateways, placement reservations, execution routing, drain, loss, and terminal state in PostgreSQL. An internal immutable execution-plan contract binds a service revision to digest-pinned environment, enforcement, provider-profile, and runtime-matrix inputs. Enforcement-bundle metadata is append-only, isolation-scoped, Rosetta-provenanced, and auditable; its deterministic logical object key is internal routing state. Internal execution admission can resolve that plan, retrieve bundle bytes through a bounded platform-object reader, reverify size and digest, reserve eligible placement, and pass owned policy bytes to the provider without accepting a caller-controlled local path. Conditional object finalization verifies immutable policy content after every write response before binding metadata and retains unreachable objects for safe retry rather than deleting across an ambiguous failure. A narrow S3 REST adapter implements those enforcement-object ports with conditional writes, checksums, safe outcome mapping, redirect denial, and an explicit operator-supplied transport. CI exercises immutable read/write, concurrent-create, finalizer lost-acknowledgement, catalog-retry, and conflict cases against a digest-pinned, anonymous, disposable SeaweedFS candidate. The recovery evidence covers object-store outage and recreation, PostgreSQL restart, eight synchronized finalizers, process and connection loss around commit, controlled promotion of a caught-up physical PostgreSQL standby behind a stable loopback client endpoint with three-round, PostgreSQL-timeline-bound health confirmation across predeclared targets, crash recovery from a durably persisted private route and timeline record, bounded conformance supervision with readiness-gated router restart, exclusive supervisor and manager ownership, contender rejection, parent-loss containment, automatic supervisor replacement, controlled manager recovery, and manager restart-budget exhaustion with fail-closed recovery, bounded reconnection of one long-lived three-connection pool, atomic convergence after primary loss during an unresolved commit, and explicit stale-primary fencing followed by rewind and read-only rejoin. It does not prove automatic election or failover, automatic fencing, transaction replay, production endpoint routing or supervision, partition safety, workload-authenticated backend selection, or production certification. New sandbox creation additionally requires an exclusive private policy workspace that reclaims only DataGround-owned crash orphans. Repair and cancellation preserve the original operation requester while persisting the current command actor and correlation as the principal for subsequent claims, audit, and consequential-effect authorization; later cancellation requests cannot replace the first accepted cancellation authority. Invocation-effect drivers can compose durable reconciliation with admission and cancellation only after resolving the persisted service, revision, current effect principal, and correlation scope and obtaining explicit consequential-effect authorization. The version-2 runtime handoff becomes available only after successful admission and resolves persisted invocation input, revision runtime settings, and current effect authority. Claimed runtime access and normalized event writes require the exact active lease owner and fencing token; bounded renewal cannot cross the operation deadline, stale workers cannot append, and the internal runtime source sequence keeps replay idempotent while conflicting reuse fails closed. A durable single-use runtime-attempt reservation consumes permission to start one native turn; successful and terminal-failure completion are fenced and replayable, deterministic runtime failure terminates with a bounded safe error, and any unresolved reservation remains ambiguous and cannot be reserved again. An opt-in claim-bound runtime driver resolves only the admitted execution, requires it to be ready, authorizes the normalized profile-specific request, renews the exact lease, persists every normalized Codex event, and completes the attempt without exposing or repeating native identities. The built-in `codex.app-server/v1` request mapper accepts a bounded persisted `prompt` plus optional immutable artifact declarations, clones the persisted output schema, and fixes locked approvals without native model or working-directory overrides. Artifact declarations are bounded, use unique portable IDs and clean absolute sandbox paths, and force workspace-write sandboxing before authorization; requests without artifacts remain read-only. The runtime driver materializes only acknowledged text deltas into a bounded declared result; structured output schemas compile before provider access without external resource loading, and each result must be exactly one conforming JSON value while malformed, non-conforming, or oversized output terminates without repeating the native turn. An invocation-artifact finalizer binds bounded immutable content to deterministic internal object keys and verifies every write outcome by exact read-back. Its PostgreSQL catalog publishes private object routing and the existing public artifact descriptor atomically only under the exact active version-2 runtime claim and reserved attempt; identical committed replay is read-only, while stale or conflicting bindings fail closed. An opt-in, explicitly size-bounded wrapper over the narrow S3 REST store implements the invocation-artifact object ports with an exact deterministic key shape, checksummed writes, safe outcome mapping, redirect denial, and the same operator-supplied transport used by enforcement objects. The runtime driver exports declared sandbox paths only after a successful native turn and valid declared output, derives isolation-scoped artifact identities, and finalizes sensitive immutable content under the exact renewed runtime claim before recording attempt success. The OpenShell adapter owns each private export destination: host paths never cross the provider port, content is bounded and non-serializable, and verified cleanup completes before bytes return. Uncertain export or publication outcomes remain ambiguous and cannot repeat the native turn. An opt-in composition boundary owns admission, claim-bound runtime execution, and cancellation together; incomplete version-2 composition fails before worker startup instead of falling back phase by phase. The reconciler forwards claim authority only to explicitly claim-bound effect routes; claimless or mismatched calls to those routes fail closed. Cancellation observes the exact persisted execution before termination, treats absence as safe only when admission was never prepared, and retains unresolved admission as retryable; the default worker and public API do not configure these paths. The internal Rosetta client covers the pinned v1 candidate HTTP contract, strict compilation, scoped provenance, sanitized failures, and independent OpenShell-policy validation; it remains unwired and production use is blocked on a tagged, certified release. The initial internal Codex adapter covers its pinned stable handshake, one ephemeral thread/turn, bounded normalized events, interruption, and fail-closed approvals. None of these components is wired to a public admission path, and the invocation-artifact S3 transport and composed object-finalizer/PostgreSQL-catalog acknowledgement recovery have disposable live evidence, but no live runtime, production object-store backend or credential non-exposure certification has passed; a bounded scanner-owned report path can inspect the seven closed credential-evidence surfaces without receiving canary plaintext, validates run and resource context before reading, records profile-owned input limits, scanner-owned observation intervals, content-free scan metrics, domain-separated inspected-input commitments, fresh run nonces, and closed live-resource identities in every report, and requires the exact reports, profile-owned verifier identity, and resource-bound cleanup receipts in final evidence, and a closed collector validates the complete seven-source plan before acquisition, opens each source once in canonical order, passes it directly through the scanner-owned report path, and sanitizes acquisition failures. An opaque run boundary owns the checked profile snapshot, verifier identity, observation interval, resource bindings, cleanup order and receipts, and final result; failed collection, uncertain cleanup, or clock regression cannot serialize as evidence. A private run-bound verifier workspace adapter creates one fresh non-serializable identity beneath a deployment-owned directory and accepts removal only for the exact run and workspace after verifying that it is empty. A separate non-serializable sandbox cleanup adapter binds the run to the exact persisted DataGround execution, terminates it through the provider boundary, and accepts removal only after an exact terminal observation. A run-derived provider cleanup adapter binds the exact OpenShell provider identity and persisted gateway, rejects replacement by name, and accepts deletion only after timestamped absence. A run-bound non-serializable source adapter owns the exact seven-source plan, portable resource bindings, typed OpenShell, Docker, and runtime ports, and one single-use collection attempt. A private OpenShell stream backend resolves the exact persisted execution to its native sandbox and gateway, revalidates both before each read, and runs four fixed no-shell inspection commands with sanitized failures. Concrete Docker and runtime source backends and Docker-hosted evidence remain blocked. `docs/development/openshell-local.md` records the exact scope. Authentication, provider credential mediation, default/public execution composition, production backend selection, and production infrastructure remain unimplemented. Cedar authorization is available only through explicit immutable policy bundles and opt-in static composition; no default policy or worker wiring is configured. Complete the governed agent-service vertical slice before integrating broad notebook, lakehouse, job, or compatibility surfaces.

## Architecture boundaries

Public clients use DataGround APIs and resource identities. They must never receive OpenShell gateway addresses, sandbox ports, Kubernetes credentials, root storage credentials, or native harness endpoints.

Cedar expresses authorization intent. Rosetta is the Cedar-to-OpenShell materialization boundary; do not add a fallback compiler inside DataGround. An unavailable or unrepresentable translation fails closed. OpenShell provides the sandbox execution and enforcement boundary, while DataGround owns product identity, authorization, durable state, placement, lifecycle, audit, and public contracts.

Provider credentials must never be readable by notebook or harness processes. Use OpenShell-mediated credentials or an explicitly designed credential-holding bridge. OpenShell provider-profile selection must resolve through the immutable deployment-owned registry; malformed, unregistered, or fallback profiles fail closed before provider access. Treat raw provider-secret visibility as a configuration and release failure.

Durable lifecycle state belongs in PostgreSQL-backed, finite resource state machines. Workers are replaceable, state transitions and outbox records commit atomically, timers and retries are durable, and ambiguous external effects are observed before repetition. Process-local state is permitted only in the explicitly documented reference implementation and must not acquire production guarantees. Do not introduce a general workflow engine or generic workflow abstraction without a new architectural decision supported by measured need.

Every resource, authorization decision, cache key, queue item, storage prefix, event, metric, and audit record is isolation-domain scoped. Trusted installations may simplify tenant concepts in the user interface, but implementation must not weaken scope separation.

Native runtime protocols remain internal. Codex, Claude Code, OpenCode, and Hermes integrate through one platform contract with explicit capability negotiation. Preserve Hermes profile, delegation, Kanban, memory, skill, schedule, and messaging boundaries rather than flattening them into generic sessions. The platform-native service and event APIs precede any OpenAI-compatible facade.

## Engineering approach

Prefer focused changes that solve the underlying problem and leave clear extension points. Avoid speculative abstractions, premature microservices, duplicated policy or lifecycle semantics, and compatibility layers without a concrete consumer. A deployable boundary must have a distinct trust, scaling, failure, runtime, or release reason.

Use prose by default in documentation, comments, commit messages, and pull request descriptions. Use lists when they communicate a real sequence or exact set more clearly than prose. Comments should explain invariants, tradeoffs, or non-obvious reasoning instead of restating code.

Write pull request descriptions as direct, concise prose for human reviewers. Explain why the change is needed and call out pivotal decisions or reviewer caveats. Do not narrate the diff, include iteration scores, or add template headings that merely label obvious paragraphs. Let configured checks report routine verification; state limitations plainly when checks do not yet exist.

Follow [Conventional Commits 1.0.0](https://www.conventionalcommits.org/en/v1.0.0/) for commit headers: `<type>[optional scope][!]: <description>`. Use a stable scope only when it adds useful context, write concise lower-case imperative descriptions without a trailing period, and document breaking changes in a `BREAKING CHANGE:` footer.

Do not provide time estimates. Describe scope, dependencies, risks, decisions, and verification evidence. Prefer durable, long-term solutions when a shortcut would create another contract, migration, security exception, or maintenance burden.

## Contracts, security, and observability

Treat API schemas, event envelopes, error codes, diagnostic codes, resource states, database migrations, audit fields, artifact descriptors, and release manifests as compatibility surfaces. Version material semantic changes and keep schemas, generated clients, examples, tests, and documentation synchronized.

Mutating commands are idempotent. External operations use deterministic identifiers. Public errors contain stable codes, safe messages, correlation identifiers, and retryability without leaking upstream payloads or secrets. Large or sensitive event content becomes a governed artifact reference rather than an unbounded inline payload.

Authorize at entry and again before consequential external effects. Test denials, malformed inputs, duplicates, cancellation, timeouts, lost acknowledgements, restarts, partial effects, stale leases, cross-domain collisions, and deletion—not only successful behavior.

Logs, traces, metrics, and audit records share correlation fields but have different retention and sensitivity contracts. Do not log tokens, prompts, notebook or artifact contents, provider responses, policy entities, or generated enforcement artifacts by default.

## Frontend expectations

Use semantic design tokens and shared accessible primitives before creating local variants. WCAG 2.2 AA is the complete-journey target. All functions must be keyboard-operable with visible focus, and color must never carry status alone.

Token source files conform to the pinned DTCG 2025.10 subset documented in `docs/development/design-system.md`. Edit source token files and run `pnpm tokens:generate`; never edit generated CSS, JavaScript, or declarations directly. Shared primitives belong in `@dataground/ui`, remain free of product authorization and data loading, and require a Storybook contract plus focused tests before adoption by the workbench.

Keep requested and observed state distinct. Render waiting, degraded, cancelling, failed, and unknown states explicitly. Event-rich interactions include tools, processes, files, questions, approvals, usage, lifecycle, and artifacts rather than reducing every runtime to chat messages. Approval interfaces must not imply authority that policy and enforcement cannot represent.

## Verification

Use the Go, Node.js, and pnpm versions pinned by `.go-version`, `.nvmrc`, and `package.json`. Install JavaScript dependencies with `pnpm install --frozen-lockfile`. Run `pnpm verify` for the repository-wide baseline: formatting, linting, contract and generated-artifact checks, pinned development-profile checks, type checking, Go, token, UI, workbench, browser-story and accessibility tests, and production builds. GitHub CI additionally runs the live enforcement-object subset against the pinned disposable backend candidate. After an accepted OpenAPI change, run `pnpm contracts:generate`; never edit generated contract types directly. Use `pnpm dev:api`, `pnpm dev:workbench`, and `pnpm dev:design-system` for the development processes.

Behavior changes require tests at the appropriate contract, integration, security, resilience, or accessibility layer. Do not claim a check passed unless it ran; report checks that could not run and why.

Before finishing substantive work, review the complete change in five passes: factual and contract accuracy; scope and instruction precedence; security and isolation; failure recovery and operability; then maintainability, documentation, and diff regression. Retain only changes that improve the outcome. Do not put pass scores or procedural notes in repository documentation, commits, or pull request descriptions.

## Maintaining this file

Use `.agents/skills/update-agents-md/SKILL.md` when creating or substantially revising agent guidance. Keep this file concise and repository-specific. Add durable instructions only after verifying them against the current tree and toolchain, remove obsolete guidance when the repository changes, and place detailed repeatable procedures in focused documentation or skills rather than expanding this file indefinitely.
