packages:
  - "apps/*"
  - "packages/*"

minimumReleaseAge: 1440

overrides:
  # Preserve each dependent package's major line while enforcing patched releases.
  brace-expansion@2.1.2: 2.1.4
  brace-expansion@5.0.7: 5.0.9
  fast-uri: 3.1.5
  js-yaml: 4.3.0

allowBuilds:
  esbuild: true
